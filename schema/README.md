# Schema

| 文件 | 说明 |
|------|------|
| `raw_export_record.schema.json` | 平台 JSONL 导出行 |
| `normalized_annotation.schema.json` | 解析 `_annotation` 后的结构 |
| `annotated_sample.schema.json` | `AnnotationSample.to_dict()` 的形状参考（可选校验） |

Metadata / QA 无 schema。
